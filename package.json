{
  "name": "safedep",
  "version": "0.1.0",
  "type": "module",
  "description": "Lightweight dependency risk scannerrrr",
  "main": "dist/src/index.js",
  "bin": {
    "safedep": "dist/src/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/src/cli/index.js",
    "dev": "tsc -w",
    "test": "npm run build && node --test dist/tests/engine.test.js"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
